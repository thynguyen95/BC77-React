import { Fragment } from "react";
// Fragment: tạo ra 1 thẻ rỗng, không render ra giao diện

// Khởi tạo 1 component
const Header = () => {
    return (
        <>
            <div>Header Component</div>
            <div>Header Component</div>
        </>
    );
};

export default Header;
